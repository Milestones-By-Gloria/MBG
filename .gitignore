# Astro / Node
site/node_modules/
site/dist/
site/.astro/
site/.env
site/.env.production

# OS / editor
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/

# Local Claude Code settings (not for shared repo)
.claude/

# Source design files (kept out of repo — they live with Gloria in Canva)
*.pdf

# Source photos (originals from Gloria; web-sized copies live under
# site/public/images/ which IS committed)
Photos to use*/

# Derived assets (regenerated from the source PDF, no need to commit)
_pdf_extract/
_design.txt
